import React, { Component } from 'react';

import GuessBar from '../containers/guess_bar';

export default class App extends Component {
  render() {
    return (
      <div>
        <GuessBar />
    
      </div>
    );
  }
}
